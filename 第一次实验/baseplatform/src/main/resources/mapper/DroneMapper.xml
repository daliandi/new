<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.md.basePlatform.mapper.DroneMapper">
    <resultMap id="droneResultMap" type="com.md.basePlatform.domain.Drone">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="model" column="model" />
        <result property="manufacturer" column="manufacturer" />
        <result property="serialNumber" column="serialNumber" />
        <result property="flightTime" column="flightTime" />
        <result property="status" column="status" />
        <result property="createdDate" column="createdDate" />
        <result property="updatedDate" column="updatedDate" />
    </resultMap>

    <select id="findAll" resultMap="droneResultMap"> SELECT * FROM drone </select>

    <select id="findById" parameterType="java.lang.Integer" resultMap="droneResultMap"> SELECT *
        FROM drone WHERE id = #{id} </select>

    <insert id="save" parameterType="com.md.basePlatform.domain.Drone" useGeneratedKeys="true"
        keyProperty="id" keyColumn="id"> INSERT INTO drone (name, model, manufacturer, serialNumber,
        flightTime, status, createdDate, updatedDate) VALUES (#{name}, #{model}, #{manufacturer},
        #{serialNumber}, #{flightTime}, #{status}, #{createdDate}, #{updatedDate}) </insert>

    <update id="update" parameterType="com.md.basePlatform.domain.Drone"> UPDATE drone SET name =
        #{name}, model = #{model}, manufacturer = #{manufacturer}, serialNumber = #{serialNumber},
        flightTime = #{flightTime}, status = #{status}, updatedDate = #{updatedDate} WHERE id =
        #{id} </update>

    <delete id="delete" parameterType="java.lang.Integer"> DELETE FROM drone WHERE id = #{id} </delete>

    <select id="search" parameterType="java.lang.String" resultMap="droneResultMap"> SELECT * FROM
        drone WHERE name LIKE #{keyword} OR model LIKE #{keyword} OR
        manufacturer LIKE #{keyword} OR serialNumber LIKE #{keyword} OR
        status LIKE #{keyword} </select>
</mapper>